# Release Output

This folder is generated by the Windows build.

Expected outputs after running `npm run build:win` or `build-release.cmd`:
- `Lighting Texture Previewer Setup <version>.exe` (installer)
- `Lighting Texture Previewer <version>.exe` (portable app)

Binary outputs in this folder are intentionally git-ignored.

